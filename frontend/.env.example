# Optional: enable connected services through Vite's local API proxies.
# Leave unset for the existing local numerical-analysis workflow.
# VITE_API_BASE_URL=/api
# Optional isolated rationale-v6 service (local Vite proxy -> 127.0.0.1:8003).
# VITE_RATIONALE_API_BASE_URL=/api-v6
