Good. It works better now. But couple things we need to addresss:



1. When I reject after the run is waiting for approval, nothing happens. The UI just stays the same and I can't iterate on the current run of the data sourcing. In addition to the iteration, the user should be able to also select other models rather than the final recommendation of the agent.